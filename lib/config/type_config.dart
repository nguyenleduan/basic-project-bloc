enum TypeCategoryFitter { ai, it, design, marketing }

enum TypeLevelFitter { all, fresher, junior, senior }

enum TypeTuitionFitter { free, payFee }

enum TypeRankFitter { one, tow, three, four, five }

enum TypeTimeFitter { oneToThree, threeToSix, sixToNine }

enum TypeStudyingFitter { studying, completed, registered,canceled }
